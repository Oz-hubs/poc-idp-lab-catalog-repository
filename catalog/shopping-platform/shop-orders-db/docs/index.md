# shop-orders-db

PostgreSQL database used by `shop-orders-service` to persist orders.

- **Type:** database (postgres)
- **Used by:** `shop-orders-service`

## Notes

- Example resource only — not provisioned anywhere.
- In a real setup, link this entity to the actual managed instance (e.g. via cloud annotations).
